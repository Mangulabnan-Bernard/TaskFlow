# OS / editor
.DS_Store
*.log

# env files (each app also ignores its own)
.env
.env*.local

# dependencies / build output (each app also ignores its own — belt-and-suspenders)
node_modules/
.next/
dist/
